app:
  title: Forge — AI-Native IDP
  baseUrl: http://localhost:3000

organization:
  name: victorz.cloud

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  csp:
    connect-src: ["'self'", 'http:', 'https:']
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  database:
    client: pg
    connection:
      host: ${POSTGRES_HOST}
      port: ${POSTGRES_PORT}
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}

auth:
  environment: development
  providers:
    guest:
      dangerouslyAllowOutsideDevelopment: false
    oidc:
      development:
        metadataUrl: ${OIDC_METADATA_URL}    # e.g. https://id.quantumapi.eu/.well-known/openid-configuration
        clientId: ${OIDC_CLIENT_ID}
        clientSecret: ${OIDC_CLIENT_SECRET}
        prompt: consent
        additionalScopes:
          - profile
          - email

integrations:
  github:
    - host: github.com
      token: ${GITHUB_TOKEN}  # Personal access token with repo scope

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration

  rules:
    - allow: [Component, System, API, Resource, Location, Template, Group, User]

  locations:
    # Local catalog entities
    - type: file
      target: ../../catalog/all.yaml

    # Templates
    - type: file
      target: ../../templates/dotnet-api/template.yaml
      rules:
        - allow: [Template]
